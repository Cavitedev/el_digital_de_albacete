import 'package:flutter/material.dart';

class NoMoreNewsFoundErrorPlaceholder extends StatelessWidget {
  
  
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('error, no more news found'),
    );
  }
}
